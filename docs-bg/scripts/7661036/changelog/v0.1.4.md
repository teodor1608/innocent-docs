# v0.1.4

Test release candidate — verifying the release workflow itself (prerelease flagging, changed-files list, `set -e` fix). Not a real content release.

### Changed files

_Since `v0.1.3`:_

**Modified**

* inn-achievements/fxmanifest.lua
